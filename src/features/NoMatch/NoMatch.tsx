import { Layout } from "components/Layout";
import React from "react";

const NoMatch = () => (
  <Layout>
    <header>
      <h1>Page Not Found</h1>
    </header>
  </Layout>
);

export { NoMatch };
